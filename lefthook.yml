pre-commit:
    commands:
        eslint-tslint:
            glob: '*.{ts,tsx}'
            run: yarn lint && yarn tsc --noEmit
        stylelint:
            glob: '*.{css}'
            run: yarn stylelint
